var srt2vtt = require('../srt-to-vtt');
var fs = require('fs');

function changesrt(dir) {

	var currentDir = dir;
	console.log('type of :' + currentDir);

fs.readdir(currentDir, function(err, files){

if (err) {
      throw err;
    }

    var tableaufichier=[];
    console.log(files);

}); 

    console.log("salut");
    console.log('dir : ' + dir);

    fs.createReadStream('Argo.srt')
        .pipe(srt2vtt())
        .pipe(

            fs.createWriteStream('Argo.vtt')


        );

}

module.exports.changesrt = changesrt;
